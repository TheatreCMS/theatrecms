<header class="bg-white border-b">
    <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
        <a href="/" class="text-2xl font-semibold text-gray-900">{{ name }}</a>
        {% if get_menu('primary') is not null %}
            {{ render_menu('primary') }}
        {% endif %}
    </div>
</header>