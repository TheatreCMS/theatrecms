{% include 'admin/partials/_alert.html.twig' with {'type': 'success', 'message': 'Season saved successfully.'} %}
{% include 'admin/partials/_featured_image_field.html.twig' with {
    entityType: 'season', entityId: season.id, featuredImageUrl: season.featuredImageUrl, featuredImageId: season.featuredImage.id|default(null), oob: true
} %}
