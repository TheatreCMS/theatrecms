{# ── Image picker modal ───────────────────────────────────────────────────── #}
<dialog id="image-picker-modal"
        class="rounded-lg shadow-xl p-0 w-full max-w-3xl open:flex open:flex-col backdrop:bg-black/50"
        aria-modal="true">
	<div id="image-picker-modal-content" class="w-full">
		{# Populated by HTMX hx-get on the "Choose Image…" buttons #}
	</div>
</dialog>
