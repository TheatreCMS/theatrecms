{# ── Media details modal ──────────────────────────────────────────────────── #}
<dialog id="media-details-modal"
        class="rounded-lg shadow-xl p-0 w-full max-w-lg open:flex open:flex-col backdrop:bg-black/50"
        aria-modal="true">
	<div id="media-details-modal-content" class="w-full">
		{# Populated by HTMX hx-get on the media grid tiles #}
	</div>
</dialog>
