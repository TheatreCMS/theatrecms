build:
    environment:
        php: '8.2'
        node: '20'

    dependencies:
        before:
            - composer install --no-interaction --prefer-dist
            - npm install

    tests:
        before:
            - npm test

checks:
    php:
        code_rating: true
        duplication: true
